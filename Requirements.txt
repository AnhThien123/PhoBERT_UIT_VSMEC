# Deep learning core
torch>=2.0.0
transformers>=4.40.0
datasets>=2.19.0
accelerate>=0.30.0
evaluate>=0.4.0
 
# Tách từ tiếng Việt (bắt buộc cho PhoBERT)
py_vncorenlp>=0.1.4
 
# Đánh giá & xử lý dữ liệu
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0
openpyxl>=3.1.0
 
# Trực quan hóa
matplotlib>=3.7.0
seaborn>=0.12.0
"""
 PhoBERT_UIT_VSMEC.ipynb (notebook Colab endto-end), 
cached_datasets chứa các cấu hình huấn luyện và dữ liệu, 
results là file chứa kết quả huấn luyện và các file .csv về kết quả các seed huấn luyện.
Hướng dẫn tái lập thực nghiệm:
Người đọc thực thi lần lượt các khối lệnh (cells) trong tệp
PhoBERT_UIT_VSMEC.ipynb để tái lập toàn bộ thực nghiệm.
"""
